{{ header }}


<section class="flat-breadcrumb">
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <ul class="breadcrumbs">
            {% for breadcrumb in breadcrumbs %}
              <li class="{% if breadcrumb.order == "last" %}trail-end {% else %}trail-item{% endif %}">
                <a href="{{ breadcrumb.href }}" title="">{{ breadcrumb.text }}</a>
                  {% if breadcrumb.order != "last" %}<span><img src="catalog/view/theme/default/images/icons/arrow-right.png" alt=""></span>{% endif %}
              </li>
            {% endfor %}
        </ul><!-- /.breacrumbs -->
      </div><!-- /.col-md-12 -->
    </div><!-- /.row -->
  </div><!-- /.container -->
</section><!-- /.flat-breadcrumb -->




<section id="account-forgotten" class="flat-account background">
  <div class="container">

      {% if error_warning %}
        <div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> {{ error_warning }}</div>
      {% endif %}

    <div class="row">

        {{ column_left }}

        {% if column_left and column_right %}
            {% set class = 'col-lg-7 col-md-4' %}
        {% elseif column_left or column_right %}
            {% set class = 'col-lg-9 col-md-8' %}
        {% else %}
            {% set class = 'col-lg-12 col-md-12' %}
        {% endif %}

      <div class="{{ class }}">
        {{ content_top }}

        <div class="form-login">
          <div class="row">
            <div class="col-md-3">&nbsp;</div>
            <div class="col-md-6">
                <div class="title">
                  <h3>{{ heading_title }}</h3>
                  <p>{{ text_email }}</p>
                </div>
                <form action="{{ action }}" method="post" enctype="multipart/form-data">
                  <div class="form-box">
                    <label for="input-email">{{ entry_email }}</label>
                    <input type="text" name="email" value="{{ email }}" placeholder="{{ entry_email }}" id="input-email" class="form-control" />
                  </div><!-- /.form-box -->
                  <div class="form-box">
                    <button type="submit" class="login">{{ button_continue }}</button>
                    <a href="{{ back }}">{{ button_back }}</a>
                  </div><!-- /.form-box -->
                </form><!-- /#form-login -->
            </div><!-- /.col-md-6 -->
            <div class="col-md-3">&nbsp;</div>
          </div><!-- /.row -->
        </div><!-- /.form-login -->

        {{ content_bottom }}
      </div>

        {{ column_right }}
    </div>
  </div><!-- /.container -->
</section><!-- /.flat-account -->

{{ footer }}